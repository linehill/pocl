# This case is a generic check for the vecadd kernel when truncates were
# _not_ added automatically by the runtime.

CHECK: -smallbuf

CHECK: vecadd

# Should be added to all three of the buffer accessess.
CHECK: trunc
CHECK: getelementptr

CHECK: trunc
CHECK: getelementptr

CHECK: trunc
CHECK: getelementptr
