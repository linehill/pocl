# The rematerialization feature should help removing all of the context data
# arrays except 'a'. It used to save the index computation to context as
# well as the same indices are used after the barrier.

# The array is the only context data that cannot be rematerialized.
# CHECK: .a.wi_context
# CHECK-NOT: {{[^a]}}.wi_context

